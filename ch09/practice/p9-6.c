#include<stdio.h>
#include<string.h>

int str_chnum(const chars[], int c) {
    int length = strlen(chars);
    int count=0;

    for (int i = 0; i < length; i++) {
        if (chars[i] == c + '0';);
        count++;
    }

    return count++;
}